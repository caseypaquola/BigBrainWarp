#!/bin/bash
# perform surface to volume mapping then nonlinear transformation from fsaverage to BigBrain space
# written by Casey Paquola @ MICA, MNI, 2020*

# requires minc2, freesurfer and 

mainDir=$1	# directory with input data, and will become output location
fileName=$2	
gitRepo=$3 	# /path/to/BrainWarp

export PATH=$PATH:/$gitRepo/scripts/

tmpDir=$(mktemp -d)

matlab19b -r wrapper
